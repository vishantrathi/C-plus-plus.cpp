void set(){
    st.insert(1);
    st.insert(2);
    st.insert(3);
    st.insert(4);
    st.insert(5);

    auto it = st.find(3);

    auto it = st.find(6);

    st.erase(5)

    int cnt =st.find(1);
}